# Roadmap

## Metadata enrichment
- Use Discogs API as the primary source for track release year + genres.
- Add MusicBrainz fallback (free, rate-limited).
- Keep SearXNG parsing as a final fallback when APIs are missing.

